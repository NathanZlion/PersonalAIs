"""
Different API versions can be created by creating a new directory in the api directory.

The new directory should have the same structure as the v1 directory and should follow the naming convention v{version_number}.
Then add the route into the app.
"""
